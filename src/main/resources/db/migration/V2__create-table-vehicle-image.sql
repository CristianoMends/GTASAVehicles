CREATE TABLE vehicle_image (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   vehicle_id BIGINT NOT NULL,
   image_url VARCHAR(255) NOT NULL,
   CONSTRAINT pk_vehicleimage PRIMARY KEY (id)
);

ALTER TABLE vehicle_image ADD CONSTRAINT FK_VEHICLEIMAGE_ON_VEHICLE FOREIGN KEY (vehicle_id) REFERENCES vehicle (id);